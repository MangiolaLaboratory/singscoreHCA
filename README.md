# HCAscore
An R package to score signatures on the HumanCellAtlas, single-cell or pseudobulk (e.g. GSEA,singscore)


```r
singsingscoreHCA(
     c("CD3G", "CD8A"),
     tissue = "lung",
     cell_type = "t_nk"
    )
```
